<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.DrawerMenu.DrawerMenuFragment"
        android:id="@+id/drawer_layout">

    <com.google.android.material.navigation.NavigationView
            android:id="@+id/navigation_view"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@color/colorDrawer"
            app:menu="@menu/drawer_menu"
            android:theme="@style/NavigationView"
            app:itemTextColor="#ffffff"
            app:itemTextAppearance="@style/TextAppearance.AppCompat.Large">

        <com.google.android.material.button.MaterialButton
                android:id="@+id/quit_board_bt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/quit_this_board_title"
                android:layout_gravity="bottom"
                android:background="@color/colorDrawer"
                android:layout_marginStart="30dp"
                android:layout_marginLeft="30dp"
                android:drawableStart="@drawable/ic_quit_board"
                android:drawableLeft="@drawable/ic_quit_board" />

    </com.google.android.material.navigation.NavigationView>




</androidx.drawerlayout.widget.DrawerLayout>